---
phase: 01-data-contract-privacy-foundations
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - config.yaml
  - src/flow/config.py
  - tests/test_config.py
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "All budgets (per-job runtime, Garmin API call cap, LLM spend, LLM model tier) live in config.yaml — never hardcoded inline"
    - "FlowSettings loads config.yaml AND env, with env taking precedence over yaml"
    - "DB keys are accepted ONLY from env (FLOW_DB_KEY / FLOW_PRIVATE_DB_KEY) — config.yaml holds no key field"
    - "BudgetGuard warns at 80% (calls a notify fn) and raises BudgetError at exactly 100%"
    - "Budget usage state persists to the budget_usage table and survives a process restart (read back from flow.db)"
    - "The LLM model tier is read from config (digest_model), never a string literal in code (PRINCIPLES §5)"
  artifacts:
    - path: "config.yaml"
      provides: "Phase-1 budgets + model tier; NO secrets"
      contains: "digest_model"
    - path: "src/flow/config.py"
      provides: "FlowSettings (pydantic-settings, yaml+env), BudgetGuard, BudgetError, budget persistence"
      contains: "class BudgetGuard"
    - path: "tests/test_config.py"
      provides: "settings precedence + key-not-in-yaml + BudgetGuard warn/block + persistence tests"
      contains: "test_budget_guard_hard_block"
  key_links:
    - from: "src/flow/config.py::FlowSettings"
      to: "config.yaml + FLOW_* env"
      via: "pydantic-settings YamlConfigSettingsSource with env-before-yaml precedence"
      pattern: "YamlConfigSettingsSource"
    - from: "src/flow/config.py::BudgetGuard"
      to: "budget_usage table in flow.db"
      via: "persist used totals across runs and read them back"
      pattern: "budget_usage"
    - from: "BudgetGuard.check"
      to: "BudgetError at 100% / notify at 80%"
      via: "pct = used/cap thresholds"
      pattern: "BudgetError"
---

<objective>
Put every budget in config and enforce it in code. Build `FlowSettings` (pydantic-settings: loads `config.yaml`, env overrides, DB keys env-ONLY), `BudgetGuard` (warn at 80%, hard-block with `BudgetError` at 100%), and persist budget usage to the `budget_usage` table so caps survive process restarts. The LLM model tier lives in config (`digest_model`), never inline. This satisfies OPS-02 in full.

Purpose: Budgets enforced in prompts are advisory; budgets enforced in code with persisted state are real. This is the Infrastructure-capacity layer's guard that every metered operation (Phase-2 Garmin sync cap, Phase-3 weekly LLM call) will call before dispatch.
Output: config.yaml, FlowSettings + BudgetGuard with persistence, and a green config/budget test suite.
</objective>

<execution_context>
@/home/user/flow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-contract-privacy-foundations/01-RESEARCH.md
@.planning/phases/01-data-contract-privacy-foundations/01-CONTEXT.md
@.planning/phases/01-data-contract-privacy-foundations/01-01-SUMMARY.md
@.planning/phases/01-data-contract-privacy-foundations/01-02-SUMMARY.md

# RESEARCH.md Pattern 3 (BudgetGuard), Pattern 4 (pydantic-settings + correct source
# order), Pitfall 2 (key-in-config), Pitfall 6 (yaml source ordering), Open Question 1
# (settings_customise_sources signature — verify at implement time, fallback documented).
# Plan 02 created the budget_usage table this plan persists to.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.yaml + FlowSettings (yaml+env, env precedence, keys env-only)</name>
  <files>config.yaml, src/flow/config.py, tests/test_config.py</files>
  <read_first>
    - .planning/phases/01-data-contract-privacy-foundations/01-RESEARCH.md (Pattern 4 FlowSettings + settings_customise_sources order; Pitfall 2 key-in-config; Pitfall 6 yaml ordering; Open Question 1 + 3 config keys scope)
    - .planning/phases/01-data-contract-privacy-foundations/01-CONTEXT.md (budgets list: runtime ≤5min, 1 Garmin sync/day, LLM ≤$0.50 CAD/day + ≤1 narrative/week; model tier in config)
    - CLAUDE.md (digest_model default claude-sonnet-4-6; never date-suffixed ids; tier in config never inline)
  </read_first>
  <behavior>
    - test_settings_load_yaml: FlowSettings reads budget values (e.g. garmin_sync_per_day, llm_spend_cap_cad_day, job_timeout_seconds, digest_model) from config.yaml
    - test_env_overrides_yaml: a FLOW_* env var (e.g. FLOW_DIGEST_MODEL) overrides the config.yaml value (env precedence per Pitfall 6)
    - test_db_key_not_in_yaml: the DB key is sourced from FLOW_DB_KEY env only; a db_key placed in config.yaml is NOT used (key field is env-only, default empty)
  </behavior>
  <action>
    Create `config.yaml` at repo root containing ONLY Phase-1 non-secret budgets + model tier (Open Question 3 — no Phase-2 stub keys): `garmin_sync_per_day: 1`, `llm_spend_cap_cad_day: 0.50`, `llm_narrative_calls_per_week: 1`, `job_timeout_seconds: 300`, `budget_warn_pct: 0.80`, `digest_model: claude-sonnet-4-6`, `digest_max_output_tokens: 2000`, `monthly_token_budget: 50000`. NO `db_key`/`private_db_key` in config.yaml (Pitfall 2). Create `src/flow/config.py` with `FlowSettings(BaseSettings)` per RESEARCH Pattern 4: `model_config = SettingsConfigDict(env_prefix="FLOW_", env_file=".env", extra="ignore")`; secret fields `db_key`/`private_db_key` = `Field(default="")` (env-only); budget fields with the config.yaml values as fallbacks; `digest_model` etc. for model tier. Implement `settings_customise_sources` with env BEFORE YamlConfigSettingsSource (Pitfall 6). Per Open Question 1: if the `settings_customise_sources` signature differs in pydantic-settings 2.14.x, fall back to `model_config = SettingsConfigDict(yaml_file="config.yaml")` and document the deviation in the SUMMARY — do NOT block. Write `tests/test_config.py` for the behavior block; tests first (red), then implement.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_config.py::test_settings_load_yaml tests/test_config.py::test_env_overrides_yaml tests/test_config.py::test_db_key_not_in_yaml -q` passes
    - `grep -Eq "db_key|private_db_key" config.yaml` returns NON-zero (no key in config.yaml)
    - `grep -q "digest_model" config.yaml` succeeds (model tier in config, not inline)
    - `uv run python -c "from flow.config import FlowSettings; s=FlowSettings(); print(s.digest_model)"` prints a model id from config
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_config.py -q -k "settings or yaml or db_key"</automated>
  </verify>
  <done>config.yaml holds all Phase-1 budgets + model tier and NO secrets; FlowSettings merges yaml+env with env precedence; DB keys are env-only; model tier is config-driven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BudgetGuard (warn 80% / hard-block 100%) with persistence to budget_usage</name>
  <files>src/flow/config.py, tests/test_config.py</files>
  <read_first>
    - .planning/phases/01-data-contract-privacy-foundations/01-RESEARCH.md (Pattern 3 BudgetGuard; DDL budget_usage table; Don't Hand-Roll: budget state must survive restarts)
    - src/flow/config.py (Task 1 — FlowSettings provides caps + warn_pct)
    - src/flow/store/db.py (Plan 01/03 — DBConnectionFactory for persistence)
    - tests/conftest.py (tmp_db_factory fixture)
  </read_first>
  <behavior>
    - test_budget_guard_warn: at 80% usage, check() invokes the notify callback exactly once with a budget-warning message; does NOT raise
    - test_budget_guard_hard_block: at exactly 100% usage, check() raises BudgetError; below 100% it does not
    - test_budget_persists_across_restart: incrementing used + persisting to budget_usage, then re-reading via a fresh factory connection, returns the accumulated total (state survives "restart")
  </behavior>
  <action>
    Add `BudgetError(Exception)` and `BudgetGuard` to `src/flow/config.py` per RESEARCH Pattern 3: `BudgetGuard(name, cap, warn_pct=0.80)`; `check(used, notify_fn=None)` computes `pct = used/cap`, raises `BudgetError` when `pct >= 1.0`, calls `notify_fn(...)` when `pct >= warn_pct` (warn-only, no raise). Add persistence helpers that write/read accumulated usage to the `budget_usage` table (period + budget_name PK; created by Plan 02) via the DBConnectionFactory — an `increment_usage(conn, period, budget_name, amount)` (UPSERT) and `read_usage(conn, period, budget_name)` so a process restart re-reads the running total (Don't Hand-Roll: module globals reset on restart). The notify_fn is injected (no notification channel is built this phase — that is Phase 2). Write `tests/test_config.py` budget tests per the behavior block using the tmp_db_factory fixture; tests first (red), then implement.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_config.py -q -k "budget"` passes (warn + hard_block + persistence)
    - A test asserts BudgetGuard raises BudgetError at used==cap and does NOT raise at used==0.99*cap
    - A test writes usage, opens a fresh connection from the factory, and reads back the accumulated total from budget_usage
    - `grep -q "budget_usage" src/flow/config.py` succeeds (persistence wired to the table)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_config.py -q</automated>
  </verify>
  <done>BudgetGuard warns at 80% and hard-blocks with BudgetError at 100%; usage persists to budget_usage and survives a simulated restart; the notify hook is injectable for Phase 2's channel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → process | Non-secret budgets/tier read from a committed file; must contain NO secrets |
| env → secrets | DB keys enter only via FLOW_* env, overriding yaml; never read from config.yaml |
| metered op → BudgetGuard | Every future Garmin sync / LLM call must pass the guard before dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Information Disclosure | DB key placed in committed config.yaml | mitigate | db_key/private_db_key are env-only `Field(default="")`; config.yaml forbidden to hold them; test_db_key_not_in_yaml + grep gate assert absence (Pitfall 2) |
| T-01-14 | Denial of Service | budget bypass / unbounded LLM or API spend | mitigate | BudgetGuard hard-blocks at 100% with BudgetError; caps read from config; usage persisted to budget_usage so restarts can't reset the counter |
| T-01-15 | Tampering | env-vs-yaml precedence inverted (yaml silently wins) | mitigate | settings_customise_sources orders env BEFORE yaml (Pitfall 6); test_env_overrides_yaml asserts env precedence |
| T-01-16 | Information Disclosure | hardcoded model id / inline tier (PRINCIPLES §5) | mitigate | digest_model lives in config.yaml; test asserts it loads from config; grep gate asserts presence in config.yaml |
| T-01-SC | Tampering | pip/uv installs (supply chain) | accept | No NEW packages in this plan (reuses Plan 01 pinned slopcheck-[OK] deps); no install task |
</threat_model>

<verification>
- `uv run pytest tests/test_config.py -q` is green
- config.yaml holds budgets + digest_model and NO secrets
- FlowSettings: env overrides yaml; DB key env-only
- BudgetGuard warns at 80%, raises BudgetError at 100%; usage persists across a fresh connection
</verification>

<success_criteria>
- OPS-02 satisfied: all budgets in config.yaml, enforced in code, warn at 80%, hard-block at 100%
- Model tier (digest_model) is config-driven, never inline (PRINCIPLES §5)
- Budget state persists to flow.db and survives process restart
</success_criteria>

<output>
Create `.planning/phases/01-data-contract-privacy-foundations/01-04-SUMMARY.md` when done.
</output>
