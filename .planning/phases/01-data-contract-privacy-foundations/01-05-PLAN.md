---
phase: 01-data-contract-privacy-foundations
plan: 05
type: execute
wave: 4
depends_on: [01-01, 01-03]
files_modified:
  - docs/adr/0001-garmin-access.md
  - docs/adr/0002-encryption.md
  - docs/adr/0003-private-namespace.md
  - tests/test_adr.py
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "docs/adr/ contains exactly three ADRs: Garmin access method, encryption choice, private-namespace isolation"
    - "the Garmin-access ADR records the community garminconnect choice and WHY the official Health API is rejected (no personal use) and garth is rejected (deprecated/broken)"
    - "the encryption ADR records SQLCipher app-level (not filesystem-only) and the sqlcipher3-wheels platform-validation RESULT (resolved on Linux x86_64 / Python 3.12)"
    - "the private-namespace ADR records the separate-file (not column-flag) isolation design and export/telemetry exclusion by construction"
    - "each ADR follows the 5-line PRINCIPLES §2 format and contains no secrets"
    - "an automated test asserts all three ADR files exist with their required decision content"
  artifacts:
    - path: "docs/adr/0001-garmin-access.md"
      provides: "ADR: Garmin access via community garminconnect"
      contains: "garminconnect"
    - path: "docs/adr/0002-encryption.md"
      provides: "ADR: SQLCipher app-level encryption + wheels validation result"
      contains: "SQLCipher"
    - path: "docs/adr/0003-private-namespace.md"
      provides: "ADR: separate private.db file isolation design"
      contains: "private.db"
    - path: "tests/test_adr.py"
      provides: "smoke test: 3 ADR files exist with required content"
      contains: "test_adr_files_exist"
  key_links:
    - from: "tests/test_adr.py"
      to: "docs/adr/0001..0003"
      via: "asserts each file exists and contains its required decision keyword"
      pattern: "docs/adr"
    - from: "docs/adr/0002-encryption.md"
      to: "Plan 01 sqlcipher3-wheels validation"
      via: "records the resolved platform-coverage result as the decision evidence"
      pattern: "sqlcipher3-wheels"
---

<objective>
Record the three never-acceptable-to-get-wrong decisions as ADRs in `docs/adr/` (5-line PRINCIPLES §2 format): the Garmin access method, the encryption choice (with the now-resolved `sqlcipher3-wheels` platform-validation result), and the private-namespace isolation design. Add a smoke test asserting all three exist with their required content. This satisfies DATA-02 and ensures Phase 2 implements these decisions from a written record rather than re-deriving them.

Purpose: ADRs are the institutional memory for the expensive-to-change decisions. They must exist BEFORE Phase 2 implements Garmin ingestion against them. Written here, after Plans 01/03 made encryption and isolation concrete, so the ADRs cite real, verified outcomes — not speculation.
Output: three 5-line ADRs + a presence/content test, no secrets.
</objective>

<execution_context>
@/home/user/flow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-contract-privacy-foundations/01-RESEARCH.md
@.planning/phases/01-data-contract-privacy-foundations/01-CONTEXT.md
@.planning/phases/01-data-contract-privacy-foundations/01-01-SUMMARY.md
@.planning/phases/01-data-contract-privacy-foundations/01-03-SUMMARY.md
@PRINCIPLES.md
@CLAUDE.md

# CONTEXT.md "ADRs (DATA-02)" block + CLAUDE.md "Alternatives Considered" give the
# exact rationale: garminconnect (official Health API excludes personal use; garth
# deprecated/broken; per-run logins risk lockouts), SQLCipher app-level (vs FS-only),
# separate private.db file. Plan 01 resolved the wheels platform flag; Plan 03 built
# the isolation design — cite those concrete results.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three ADRs (Garmin access, encryption, private-namespace) in 5-line format</name>
  <files>docs/adr/0001-garmin-access.md, docs/adr/0002-encryption.md, docs/adr/0003-private-namespace.md</files>
  <read_first>
    - .planning/phases/01-data-contract-privacy-foundations/01-CONTEXT.md (ADRs (DATA-02) block — exact decisions + rationale)
    - CLAUDE.md (Alternatives Considered / What NOT to Use — garth deprecated, official Health API no personal use, pysqlcipher3 superseded)
    - PRINCIPLES.md (§2: significant choices get a 5-line ADR in docs/adr/)
    - .planning/phases/01-data-contract-privacy-foundations/01-RESEARCH.md (Summary finding 1: wheels resolved on Linux x86_64 Python 3.11/3.12; Assumptions A1 macOS arm64)
    - .planning/phases/01-data-contract-privacy-foundations/01-03-SUMMARY.md (private-namespace isolation as built — cite the structural separation)
  </read_first>
  <action>
    Create `docs/adr/` and author three ADRs, each in the 5-line PRINCIPLES §2 format (Title / Status / Context / Decision / Consequences). (1) `0001-garmin-access.md`: Decision = use community `garminconnect`; Context = official Garmin Health API excludes personal use (legal-entity application + license fees), `garth` is deprecated/broken by Garmin's 2025 auth change, per-run logins risk 48h+ account lockouts; Consequences = personal-use OAuth, tokens persist outside repo (Phase 2), one-file adapter swap. (2) `0002-encryption.md`: Decision = SQLCipher app-level full-DB AES-256 (not filesystem-only); Context = a stolen/synced/copied DB file must be useless without the key, FS encryption only protects against device theft; record the RESOLVED `sqlcipher3-wheels` 0.5.7 platform-validation result (works Python 3.11/3.12 Linux x86_64 per Plan 01) and note macOS arm64 remains [ASSUMED] with the `sqlcipher3` + system libsqlcipher fallback; Consequences = PRAGMA-key-first factory is the single enforcement point, WAL/sidecars encrypted. (3) `0003-private-namespace.md`: Decision = physically separate `data/private.db` with its own key (never an `is_private` column flag); Context = one missing WHERE clause must not leak spiritual-practice data; Decision/Consequences = export uses an allowlist excluding private.db by construction, private factory reachable only via explicit opt-in (cite Plan 03). No secrets, no key values in any ADR.
  </action>
  <acceptance_criteria>
    - `test -f docs/adr/0001-garmin-access.md && test -f docs/adr/0002-encryption.md && test -f docs/adr/0003-private-namespace.md` succeeds
    - `grep -qi "garminconnect" docs/adr/0001-garmin-access.md && grep -qi "personal use" docs/adr/0001-garmin-access.md` succeeds
    - `grep -qi "SQLCipher" docs/adr/0002-encryption.md && grep -qi "sqlcipher3-wheels" docs/adr/0002-encryption.md` succeeds
    - `grep -qi "private.db" docs/adr/0003-private-namespace.md && grep -qi "column" docs/adr/0003-private-namespace.md` succeeds
    - no ADR contains a value assigned to FLOW_DB_KEY (`grep -rq "FLOW_DB_KEY *=" docs/adr/` returns non-zero, or only references the var name)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/adr/0001-garmin-access.md && test -f docs/adr/0002-encryption.md && test -f docs/adr/0003-private-namespace.md && grep -qi sqlcipher3-wheels docs/adr/0002-encryption.md && echo ADRS_OK</automated>
  </verify>
  <done>Three 5-line ADRs exist recording Garmin access, encryption (with resolved wheels validation), and private-namespace isolation; rationale matches CONTEXT/CLAUDE; no secrets present.</done>
</task>

<task type="auto">
  <name>Task 2: ADR presence + content smoke test</name>
  <files>tests/test_adr.py</files>
  <read_first>
    - .planning/phases/01-data-contract-privacy-foundations/01-RESEARCH.md (Validation Architecture: test_adr_files_exist smoke test)
    - docs/adr/ (Task 1 — files to assert on)
  </read_first>
  <action>
    Create `tests/test_adr.py` with `test_adr_files_exist` asserting all three ADR files are present under `docs/adr/`, and `test_adr_required_content` asserting each contains its required decision keyword (0001→`garminconnect`, 0002→`SQLCipher` + `sqlcipher3-wheels`, 0003→`private.db`) and the 5-section ADR structure (Status/Context/Decision/Consequences headings present). This makes DATA-02 machine-verifiable so a future edit that guts an ADR fails CI.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_adr.py -q` passes
    - `test_adr_files_exist` fails if any of the three files is removed (verifiable by the assertions referencing all three paths)
    - `test_adr_required_content` asserts each required keyword is present
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_adr.py -q</automated>
  </verify>
  <done>DATA-02 is machine-verified: the three ADRs must exist with required content for the suite to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR docs → repo | Committed decision records must contain rationale but NO secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Information Disclosure | a key/secret value pasted into an ADR as an "example" | mitigate | ADRs reference env-var NAMES only; acceptance asserts no FLOW_DB_KEY value assignment in docs/adr/ |
| T-01-18 | Repudiation | undocumented expensive decision (Garmin/encryption/namespace) drifts in Phase 2 | mitigate | Three ADRs record each decision + rationale; test_adr.py blocks CI if an ADR is removed or gutted |
| T-01-SC | Tampering | pip/uv installs (supply chain) | accept | No NEW packages in this plan (docs + a test using existing pytest); no install task |
</threat_model>

<verification>
- `uv run pytest tests/test_adr.py -q` is green
- docs/adr/ holds exactly the three required ADRs with required content and no secrets
- each ADR is in 5-line PRINCIPLES §2 format
</verification>

<success_criteria>
- DATA-02 satisfied: ADRs recorded for Garmin access, encryption choice, and private-namespace isolation, machine-verified, with no secrets in source/commits
</success_criteria>

<output>
Create `.planning/phases/01-data-contract-privacy-foundations/01-05-SUMMARY.md` when done.
</output>
